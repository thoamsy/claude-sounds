{
  "name": "claude-sounds",
  "version": "0.2.0",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "claude-sounds": "index.ts"
  },
  "scripts": {
    "build": "bun build ./index.ts --compile --outfile dist/claude-sounds",
    "build:all": "bun run scripts/build-all.sh"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/core": "^1.2.0",
    "@clack/prompts": "^1.2.0",
    "picocolors": "^1.1.1"
  }
}
